this.config(['cloudinaryProvider', function (cloudinaryProvider) {
    cloudinaryProvider
        .set("cloud_name", "dee6pfpam")
        .set("secure", true)
        .set("upload_preset", "my_preset");
  }]);